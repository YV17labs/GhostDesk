# Rust
/target/
**/*.rs.bk

# Coverage (cargo-llvm-cov, via `nestrs run test cov`)
*.profraw
*.profdata
/coverage

# Secrets / local state — the `.local` tier of the .env cascade
.env*.local
tls/

# Editor / agent
.claude/
.DS_Store
